from django.db import models
# from endereco.models import endereco
from tipo_logradouro.models import TipoLogradouro
# from nome_logradouro.models import nome_logradouro


# class logradouro(endereco):
# class logradouro():
    # tipoLogradouro = models.ForeignKey(tipo_logradouro, verbose_name=u"Tipo de logradouro")
    # nomeLogradouro = models.ForeignKey(nome_logradouro, verbose_name=u"Nome do logradouro")
